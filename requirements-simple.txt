# Core dependencies only (no pandas/pyarrow)
# LangGraph and LangChain dependencies (latest versions)
langgraph>=1.0.5
langchain>=0.3.26
langchain-core>=0.3.26

# Type checking support
typing-extensions>=4.8.0

# Streamlit for web UI (minimal version)
streamlit>=1.28.0 --no-deps
altair<5
blinker>=1.0.0
cachetools>=4.0
click>=7.0
importlib-metadata>=1.4
numpy
pillow>=6.2.0
protobuf>=3.12,<5
pytz
requests>=2.18
rich>=10.14.0
tenacity>=8.1.0,<9
toml
tornado>=6.0.3
tzlocal>=1.1
validators>=0.2

# Optional: LLM providers (choose one based on your needs)
openai>=1.0.0
# anthropic>=0.7.0
# langchain-openai>=0.3.0

# Development and testing (optional)
pytest>=7.4.0
black>=23.9.0